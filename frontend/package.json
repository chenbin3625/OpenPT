{
  "name": "openpt-web",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "description": "OpenPT 监控面板：零运行时依赖的原生 Web UI",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/serve.js",
    "build": "node scripts/build.js",
    "preview": "node scripts/serve.js --preview"
  }
}
